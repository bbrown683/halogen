use ash::version::{InstanceV1_0, DeviceV1_0};
use ash::vk;
use super::instance::Instance;

pub struct Device {

}

impl Drop for Device {
    fn drop(&mut self) {

    }
}

impl Device {
    pub fn new(instance : &mut Instance) {

    }
}